import React from 'react'

export const metadata = {
  title: "Master Products | Dinemaster",
};

function layout({ children }) {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
