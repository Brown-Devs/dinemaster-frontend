import React from 'react'

export const metadata = {
  title: "Admins | Dine Master",
};

function layout({ children }) {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
