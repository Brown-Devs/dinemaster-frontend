import React from 'react'

export const metadata = {
  title: "Sub Admins | Dine Master",
};

function layout({ children }) {
  return (
    <div>
      {children}
    </div>
  )
}

export default layout
